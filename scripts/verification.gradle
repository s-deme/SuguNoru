// Isolate emulator checks from an installed personal copy and its saved routes.
allprojects {
    afterEvaluate { project ->
        if (project.plugins.hasPlugin('com.android.application')) {
            project.android.defaultConfig.applicationId = 'jp.sugunoru.app.verification'
        }
    }
}
